生成用于ssh登录的证书：
    ssh-keygen -t rsa 
上传证书到服务器
    cat ~/.ssh/id_rsa.pub | ssh git@172.16.1.19 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys" 
下载仓库 git clone git@172.16.1.19:/home2/git/imgdock.git